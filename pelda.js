var numbers = [3, 5, 8, 12, -6, 9, 5, ];

function keressHusznalNagyobb(numbers) {
    var i
    for (i = 0; i < numbers.length; i++) {
        if (numbers[i] > 20) {
            eredmeny = numbers[i]
            break
        }
    }
    return eredmény
}
}